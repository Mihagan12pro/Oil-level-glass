<UserControl x:Class="KompasWPF.UserControls.FormFooter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KompasWPF.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="0.2*"></ColumnDefinition>
            <ColumnDefinition Width="0.12*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button Name="BtHelp" Style="{StaticResource HelpButton}" Width="20" Height="20" HorizontalAlignment="Left" Grid.Column="0"></Button>
        <Button Name="BtCancel" Style="{StaticResource FormButton}" Width="60" Height="20" HorizontalAlignment="Right" Content="Отмена" Grid.Column="1"></Button>
        <Button Name="BtOk" Style="{StaticResource FormButton}" Width="60" Height="20" HorizontalAlignment="Right" Content="Ок" Grid.Column="2"></Button>
    </Grid>
</UserControl>
