<UserControl x:Class="KompasWPF.UserControls.SavingEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KompasWPF.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400" MinHeight="100" x:Name="editor">
    <Grid>
        <GroupBox Name="gbPartSaving" Foreground="Black" MinWidth="400" Margin="0,0,-116,0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="0.25*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" BorderBrush="Black" BorderThickness="1" Height="18">
                        <TextBlock>
                            
                        </TextBlock>
                    </Border>
                    <Button 
                        Grid.Column="1"
                        Margin="10 0 0 0" 
                        MaxHeight="21"
                        MinHeight="21" 
                        Style="{StaticResource FormButton}"
                        Content="Выбрать...">
                    </Button>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="0.25*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Height="18">

                    </TextBox>
                    <Label 
                        Content="Обозначение"
                        Height="24"
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center"
                        FontSize="12"
                        Grid.Column="1"
                        Margin="10 0 0 0" />
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="0.25*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Height="18">

                    </TextBox>
                    <Label 
                        Content="Наименование"
                        Padding="0"
                        Height="18"
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center"
                        FontSize="12"
                        Grid.Column="1"
                        Margin="10 0 0 0" />
                </Grid>

            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
