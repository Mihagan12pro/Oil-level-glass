<windows:NoneWindow x:Class="Oil_level_glass_UI.Windows.Validation.NoProblemsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Oil_level_glass_UI.Windows.Validation"
        xmlns:windows="clr-namespace:Oil_level_glass_UI.Windows"
        mc:Ignorable="d"
        Title="NoProblemsWindow" 
        Height="100"
        Width="250"
        ResizeMode="NoResize" 
        ShowInTaskbar="False">
    <Border BorderBrush="Black" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Border 
                Grid.Row="0"
                BorderBrush="Black"
                BorderThickness="0 0 0 1">
                <StackPanel 
                    Height="20" 
                    Background="#E4E4E4"
                    Name="WindowHeader">
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="1">
                <StackPanel.Resources>
                    <Style TargetType="TextBlock" x:Name="stackPanelTextBlock">
                        <Setter Property="HorizontalAlignment" Value="Center"></Setter>
                        <Setter Property="VerticalAlignment" Value="Center"></Setter>
                    </Style>
                </StackPanel.Resources>
                <TextBlock 
                    FontSize="14"
                    Foreground="Green"
                    Text="Проблем нет!">
                </TextBlock>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <StackPanel.Resources>
                        <Style TargetType="TextBlock" x:Name="localStyle">
                            <Setter Property="FontSize" Value="8"></Setter>
                        </Style>
                    </StackPanel.Resources>
                    <TextBlock Text="Окно само закроется через ">
                        
                    </TextBlock>
                    <TextBlock MinWidth="10" Text="{Binding Seconds}"></TextBlock>
                    <TextBlock Text="с"></TextBlock>
                </StackPanel>
            </StackPanel>

            <Button
                Grid.Row="2"
                Style="{StaticResource FormButton}"
                HorizontalAlignment="Right" 
                Height="16" 
                Width="50" 
                Margin="5"
                Content="Закрыть"
                FontSize="10"
                Command="{Binding CloseCommand}">
                
            </Button>
        </Grid>
    </Border>
</windows:NoneWindow>
