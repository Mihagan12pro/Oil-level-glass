<kompas:KompasWindow x:Class="Oil_level_glass_UI.Windows.Wizard3D.Wizard3DWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Oil_level_glass_UI.Windows.Wizard3D"
        xmlns:kompas="clr-namespace:KompasWPF;assembly=KompasWPF"
        xmlns:converter="clr-namespace:Oil_level_glass_UI.Converters"
        xmlns:user="clr-namespace:KompasWPF.UserControls;assembly=KompasWPF"
        mc:Ignorable="d"
        Title="МАСТЕР ТРЕХМЕРНОЙ МОДЕЛИ" 
        Height="850"
        Width="1000" 
        Style="{DynamicResource KompasWindowStyle}" ResizeMode="CanMinimize">
    <Window.Resources>
        <!--<converter:AppearanceConverter x:Key="AppearanceConverter">

        </converter:AppearanceConverter>-->
    </Window.Resources>

    <Grid>
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="Padding" Value="0"></Setter>
            </Style>
            <Style 
                TargetType="Button" 
                BasedOn="{StaticResource FormButton}" 
                x:Key="wizard3dStyle">
                <Setter 
                    Property="Width" 
                    Value="60">
                </Setter>
                <Setter 
                    Property="Height" 
                    Value="20">
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition>

            </RowDefinition>
            <RowDefinition Height="0.1*">

            </RowDefinition>
        </Grid.RowDefinitions>

        <Grid Name="grdClient" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Border 
                Grid.Column="0"
                Margin="20 25 20 20"
                BorderBrush="Black"
                BorderThickness="1">
                <Image 
                    Name="imgGlass"
                    Source="/Resources/Images/Glass.png"
                    Stretch="Fill">
                </Image>
            </Border>
            <Grid
                Grid.Column="1"
                Margin="0 20 20 20">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                
                <GroupBox
                    Grid.Row="0"
                    Margin="0 0 0 5"
                    Header="Размеры линзы">
                    <Grid Margin="20">
                        <Grid.Resources>
                            <Style 
                                TargetType="Label">
                                <Setter 
                                    Property="VerticalAlignment"
                                    Value="Center">
                                </Setter>
                                <Setter
                                    Property="HorizontalAlignment"
                                    Value="Left">
                                </Setter>
                            </Style>
                            <Style 
                                TargetType="TextBox">
                                <Setter
                                    Property="Width"
                                    Value="200">
                                </Setter>
                                <Setter
                                    Property="VerticalAlignment"
                                    Value="Center">
                                </Setter>
                                <Setter 
                                    Property="HorizontalAlignment"
                                    Value="Left">
                                </Setter>
                            </Style>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.3*"></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <Label 
                            Content="Высота:" 
                            Grid.Row="0"
                            Grid.Column="0">
                        </Label>
                        <TextBox  
                            Grid.Row="0"
                            Grid.Column="1" 
                            Text="{Binding Glass.Height, UpdateSourceTrigger=PropertyChanged}">
                        </TextBox>

                        <Label 
                            Content="Диаметр:" 
                            Grid.Row="1"
                            Grid.Column="0">
                        </Label>
                        <TextBox 
                            Grid.Row="1" 
                            Grid.Column="1">
                        </TextBox>
                    </Grid>
                </GroupBox>

                <user:MaterialChooser 
                     Margin="0 5"
                    Header="Материал линзы"
                    Grid.Row="1">
                </user:MaterialChooser>

                <user:SavingEditor
                     Margin="0 5"
                    Header="Сохранение линзы"
                    Grid.Row="2">
                </user:SavingEditor>

                <user:AppearanceEditor 
                     Margin="0 5 0 0"
                    Header="Внешний вид линзы"
                    Grid.Row="3">
                </user:AppearanceEditor>
            
            </Grid>
        </Grid>
        <Border Margin="0 5 0 0" Grid.Row="1" BorderThickness="0 1 0 0" BorderBrush="Gray">
            <StackPanel 
                 HorizontalAlignment="Right"
                 VerticalAlignment="Center"
                 Orientation="Horizontal"
                 Margin="0 0 20 0">
                <StackPanel.Resources>
                    <Style TargetType="Button" BasedOn="{StaticResource wizard3dStyle}">

                    </Style>
                </StackPanel.Resources>
                <Button Content="Назад" Margin="0 0 10 0">

                </Button>
                <Button Content="Дальше">

                </Button>
            </StackPanel>
        </Border>
    </Grid>
</kompas:KompasWindow>
